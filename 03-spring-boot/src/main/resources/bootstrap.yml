spring:
  application:
    name: myApp
  cloud:
    consul:
      host: 192.168.60.11
      port: 8500
      enabled: true
      config:
        enabled: true
        prefix: config
        defaultContext: myApp
        # profileSeparator: '::'
      discovery:
        enabled: false