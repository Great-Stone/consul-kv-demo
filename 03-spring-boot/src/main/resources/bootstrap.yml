spring:
  application:
    name: myApp
  cloud:
    consul:
      host: localhost
      port: 8500
      enabled: true
      config:
        enabled: true
        prefix: config
        defaultContext: myApp
        # profileSeparator: '::'
      discovery:
        enabled: false